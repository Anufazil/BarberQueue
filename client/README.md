# Frontend

See the repository root README for setup, private queue links, backend configuration, deployment and verification. Run `npm ci`, `npm run dev`, `npm run lint`, and `npm run build` from this folder. Vite's native configuration loader is used for cross-platform native-plugin loading.
